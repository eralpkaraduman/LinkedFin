node_modules
.DS_Store
dist
*.local
.env
.tanstack
.wrangler
.fallow
coverage
todos.json

# Generated by pnpm og:generate (which runs first in pnpm build)
functions/og-data.json
public/sitemap.xml
# Content-addressed copy of public/fish.db. The unhashed original IS tracked —
# it is the source of truth (see AGENTS.md); only the derived copy is ignored.
public/fish-*.db
.generated

# Dictionaries (reference data, not deployed)
dictionaries/

# Agent worktrees (created by EnterWorktree / isolated subagents)
.claude/worktrees/

# Local settings
.claude/settings.local.json
