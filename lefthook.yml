pre-commit:
  parallel: false
  commands:
    check:
      run: pnpm check
    db-integrity:
      run: pnpm db:validate

pre-push:
  parallel: true
  commands:
    fallow:
      run: pnpm fallow
    test:
      run: pnpm test:coverage
